# OrangeHRM - LOCAL environment (Dockerized instance)
# Target for a self-hosted OrangeHRM container - see Phase 9 (Docker).
# NOTE: URL is a placeholder until the container exists. The 'local'
# environment is not runnable yet; the env-switching mechanism is.
base.url=http://localhost:8080
browser=chrome
explicit.wait.seconds=10